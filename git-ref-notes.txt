Start here...
Fork me on github
A new change does not mean always a change...


git init
git clone URL
git add filename
git commit -m 'comments notes'
git status
git push origin development/master
git pull origin development/master
git config --global user.name
git config --global user.email
git rm
git log -{n}
git checkout filename
git checkout {n} filename

git remote -v

git checkout -